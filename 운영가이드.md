*Version: v1.0 (2026-05-20)*

# 운영가이드 — AI 원고실 + Zettel Bridge 모노레포

> 이 문서는 에이전트(Hermes/Claude/Codex)와 운영자가 별도 질문 없이 이 레포를 다룰 수 있도록 SSOT로 관리한다.

---

## 1. 프로젝트 정체성

| 항목 | 값 |
|---|---|
| **소스 루트** | `/Users/dongchanyoon/Documents/Work/Projects/14.zettel-writer-connect` |
| **AI 원고실 plugin id** | `ai-manuscript-studio` |
| **Zettel Connect plugin id** | `zettel-connect` |
| **AI 원고실 버전** | `0.1.0` (packages/obsidian-plugin/manifest.json) |
| **Zettel Connect 버전** | `0.1.0` (packages/zettel-connect/manifest.json) |
| **활성 설치 경로** | `~/.local/obsidian-plugins/ai-manuscript-studio/` |

---

## 2. 범위 / 경계

- **이 레포** = AI 원고실 소스 진실 원천. 이 레포에서만 AI 원고실 코드를 수정한다.
- **`/Users/dongchanyoon/Documents/Work/Projects/13.zettel-connect`** = Zettel Connect 독립 레포. W4 태스크에서 명시적으로 허가하지 않으면 **절대 수정 금지**.
- `packages/zettel-connect/` 안의 Zettel Connect 번들은 이 레포에서 빌드하는 사본이다. 13.zettel-connect 원본과 동기화할 때만 업데이트.
- `apps/desktop/` = 레거시 Tauri 앱. Phase 5 검증 후 폐기 예정. 빌드/배포하지 않는다.

---

## 3. 패키지 맵

```
packages/
  core/               # @ai-manuscript-studio/core — 공유 비즈니스 로직, 스키마, IO
  obsidian-plugin/    # @ai-manuscript-studio/obsidian-plugin — AI 원고실 Obsidian 플러그인
  zettel-connect/     # zettel-connect — Zettel Connect Obsidian 플러그인
apps/
  desktop/            # 레거시 Tauri 앱 (폐기 예정)
```

---

## 4. 핵심 명령어

### 의존성 설치

```bash
pnpm install
```

### 테스트

```bash
# targeted: obsidian-plugin structure bridge + main command
pnpm --filter @ai-manuscript-studio/obsidian-plugin test -- --runTestsByPath \
  tests/structureBridge.test.ts tests/mainStructureBridgeCommand.test.ts
# → 29/29 PASS (W3A path-rejection 3개 포함)

# full: obsidian-plugin
pnpm --filter @ai-manuscript-studio/obsidian-plugin test
# → 268/268 PASS

# core
pnpm --filter @ai-manuscript-studio/core test

# 전체 (모든 패키지)
pnpm test
```

### 빌드

```bash
# obsidian-plugin 단독 (tsc -noEmit + esbuild production)
pnpm --filter @ai-manuscript-studio/obsidian-plugin build

# 빌드 산출물 문법 확인
node --check packages/obsidian-plugin/main.js

# 전체 플러그인 빌드
pnpm build:plugins
```

### 배포

```bash
# AI 원고실만 배포
pnpm deploy:ai-manuscript

# Zettel Connect만 배포
pnpm deploy:zettel

# 전체 배포
pnpm deploy
```

**환경 변수 오버라이드**:

```bash
OBSIDIAN_PLUGIN_INSTALL_ROOT=/path/to/custom/install \
OBSIDIAN_VAULT_PLUGINS_DIR=/path/to/vault/.obsidian/plugins \
pnpm deploy:ai-manuscript
```

> **주의**: 빌드/소스 변경은 Obsidian에 자동 반영되지 않는다. 배포 후 Obsidian에서 `Cmd+P → 다시 로드 (개발자용)` 또는 플러그인 off/on이 필요하다. 대표님이 명시적으로 배포를 요청할 때만 `deploy` 명령을 실행한다.

---

## 5. 현재 Zettel Bridge 워크플로우 (W1–W3 완료)

### W1 — 활성 구조노트 가져오기

- **커맨드 id**: `import-active-structure-note`
- **이름**: `현재 구조노트를 원고 프로젝트로 가져오기`
- **동작**: 현재 활성 파일이 `3.Structure/*.md`인 경우 frontmatter/H1/주장을 추출해 `4.Writing/<slug>/project.json · binder.json · planning.md` 생성.
- **안전**: `3.Structure/` 외 파일에서 실행하면 Korean warn notice 후 no-op.

### W2 — `project.json.sourceNotes` AI 컨텍스트 주입

- `ContextComposer`가 scene의 sibling `project.json.sourceNotes`를 v2 canonical source list로 읽어 AI 코칭 컨텍스트에 주입.
- `buildSelectionPrompt`에 optional `sourceNotesContext` 인자 추가(하위 호환).

### W3 — `_index/writing-handoff.json` 가져오기

- **커맨드 id**: `import-writing-handoff-json`
- **이름**: `원고실 handoff JSON 가져오기`
- **동작**: `_index/writing-handoff.json`을 읽어 `parseWritingHandoffJson` → `createWritingProjectFromHandoff` 실행.
- **안전**: JSON 없음 / 파싱 실패 → Korean notice 후 no-op. 절대 경로 및 `..` 경로는 파싱 시 reject.

### `project.json.sourceNotes` 구조

```json
{
  "sourceNotes": [
    "3.Structure/전략-노트.md",
    "2.Permanent/A.md",
    "2.Permanent/B.md"
  ]
}
```

순서: `structureNotePath` 먼저, picked 영구노트가 이어짐. 중복 order-preserving de-dupe.

---

## 6. Handoff JSON 계약 예시 (`_index/writing-handoff.json`)

```json
{
  "version": 1,
  "mode": "new-structure-to-writing",
  "structureNote": {
    "path": "3.Structure/헤지펀드-전략.md",
    "title": "헤지펀드 포트폴리오 전략",
    "id": "S-HF-001",
    "claim": "분기별 리밸런싱이 장기 수익률을 높인다"
  },
  "picked": [
    { "path": "2.Permanent/리밸런싱-연구.md", "id": "P-001" },
    { "path": "2.Permanent/수익률-분석.md",    "id": "P-002" }
  ],
  "project": {
    "title": "헤지펀드 전략 보고서",
    "genre": "investment-strategy-memo",
    "wordGoal": 5000,
    "status": "planning"
  },
  "targetWritingFolder": "4.Writing/Reports"
}
```

**필드 규칙**:
- 모든 `path` 값은 vault-relative POSIX 경로 (절대 경로 `/` 시작, `..` 포함 시 reject).
- `project`, `picked`, `targetWritingFolder`는 선택 필드.
- `structureNote.path` 필수.

---

## 7. 완료 전 검증 체크리스트

```bash
# 1. targeted 테스트
pnpm --filter @ai-manuscript-studio/obsidian-plugin test -- \
  --runTestsByPath tests/structureBridge.test.ts tests/mainStructureBridgeCommand.test.ts
# 기대: 29/29 PASS

# 2. full 테스트
pnpm --filter @ai-manuscript-studio/obsidian-plugin test
# 기대: 268/268 PASS (W3A 이후 기준)

# 3. 빌드
pnpm --filter @ai-manuscript-studio/obsidian-plugin build

# 4. 문법 확인
node --check packages/obsidian-plugin/main.js

# 5. whitespace 확인
git diff --check

# 6. 금지 경로 미수정 확인
git status --short
# 13.zettel-connect / vault / deploy scripts / _index / 2.Permanent / 3.Structure 에 M/A 없어야 함
```

---

## 8. Git / Push 워크플로우

- **upstream origin**: 현재 계정에서 read-only. 직접 push 불가.
- **퍼블리시 경로**: fork 브랜치 `feat/ai-manuscript-studio-workflow-20260519` → PR #2.
- **검증 명령**:

```bash
# 로컬 HEAD와 fork 브랜치가 동기화됐는지 확인 (기대: 0 0)
git rev-list --left-right --count HEAD...fork/feat/ai-manuscript-studio-workflow-20260519

# fork 브랜치에 push (사전 승인 후에만)
git push fork HEAD:feat/ai-manuscript-studio-workflow-20260519

# PR #2 head SHA와 로컬 HEAD 비교
gh pr view 2 --json headRefOid -q '.headRefOid'
git rev-parse HEAD
```

> **중요**: push/PR 작업은 반드시 사전 승인 후 실행. 대표님이 명시적으로 "push해"라고 하지 않으면 실행 금지.

---

## 9. 런타임 / 배포 주의사항

| 규칙 | 이유 |
|---|---|
| 소스 수정 후 자동 반영 X | esbuild 번들 후 deploy 명령 + Obsidian 리로드 필요 |
| 소스 작업 중 vault 파일 직접 쓰기 금지 | vault는 사용자 실제 데이터 — 사고 위험 |
| deploy는 명시적 승인 후에만 | C0.1 SHA-256 일치 확인 패턴 유지 |
| `apps/desktop/` 빌드 금지 | 레거시, 아직 폐기 전 보존만 |
| `OBSIDIAN_VAULT_PLUGINS_DIR` 기본값 확인 | deploy 스크립트 기본값이 `futurewave` 경로. 대표님 환경에서는 환경 변수 오버라이드 필요 |

---

## 10. 트러블슈팅

| 증상 | 원인 / 해결 |
|---|---|
| handoff JSON 없음 → notice 후 no-op | 정상 동작. `_index/writing-handoff.json` 생성 전 상태 |
| `vault-relative path여야 합니다` 오류 | JSON 안의 path 값이 `/`로 시작하거나 `..`을 포함함. 경로 수정 필요 |
| AI CLI 미작동 | AI CLI 설정(codex binary 경로 등)은 로컬 환경 설정. 레포에 API 키 없음 |
| `dist` / `main.js` 없음 오류 | 빌드 미실행 상태. `pnpm build:plugins` 후 재시도 |
| Obsidian에서 커맨드 안 보임 | 배포 후 Obsidian 플러그인 off/on 또는 개발자용 다시 로드 필요 |
| untracked 빌드/스테이징 산출물이 보임 | `git status --short` 확인 후 의도된 소스/docs/harness 증거만 스테이징. 정체 불명 산출물은 commit 전 검토 필수 |

---

*참조 문서*: `docs/planning/06-tasks.md` (태스크 현황) · `CLAUDE.md` (에이전트 규칙) · `docs/planning/b3-register-voice-spec.md` (register-voice 설계)
